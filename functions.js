// Functions, functions(), => Functions.

// We can pass a string as an argument.
// Console.log("I am really tired.")

// We can also pass the binding to a function.

const catName = "Brett";

//console.log("`My Cat\'s name is ${catName})";)

console.log(Math.pow(2,6));

const volumeOfCube = function (length) {
    return length * length * length;
}

// Old school function.
function a() {

};

// Arrow function.
// Declare the name of the function.
// X is the variable you input.
// Everything after the arrow is times 2.
let d = x => x * 2;

